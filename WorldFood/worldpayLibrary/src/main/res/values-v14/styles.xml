<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme for API 14+. This theme completely replaces
        AppBaseTheme from BOTH res/values/styles.xml and
        res/values-v11/styles.xml on API 14+ devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light">

        <!-- API 14 theme customizations can go here. -->
        <item name="android:homeAsUpIndicator">@drawable/ic_ab_back_holo_dark</item>
        <item name="android:actionBarStyle">@style/WorldPayTheme.ActionBarStyle.Light</item>
    </style>

    <style name="AppBaseThemeDark" parent="android:Theme.Holo.Light">

        <!-- API 14 theme customizations can go here. -->
        <item name="android:homeAsUpIndicator">@drawable/ic_ab_back_holo_light</item>
        <item name="android:actionBarStyle">@style/WorldPayTheme.ActionBarStyle.Dark</item>
    </style>

    <!-- Light style -->
    <style name="WorldPayTheme.ActionBarStyle.Light" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:displayOptions">homeAsUp|showHome|showTitle</item>
        <item name="android:titleTextStyle">@style/WorldPayTheme.ActionBar.Light.TitleTextStyle
        </item>
        <item name="android:background">#ffffff</item>
    </style>

    <style name="WorldPayTheme.ActionBar.Light.TitleTextStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">#000000</item>
    </style>

    <!-- Dark styles -->
    <style name="WorldPayTheme.ActionBarStyle.Dark" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:displayOptions">homeAsUp|showHome|showTitle</item>
        <item name="android:titleTextStyle">@style/WorldPayTheme.ActionBar.Dark.TitleTextStyle
        </item>
        <item name="android:background">#333333</item>
    </style>

    <style name="WorldPayTheme.ActionBar.Dark.TitleTextStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">#ffffff</item>
    </style>

</resources>